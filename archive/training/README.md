# Archived training experiments

Superseded `hero_rl_train_*` experiment scripts (v2–v19, v12x), moved here
2026-09-12 to declutter the repo root. Each file is a self-contained,
versioned training run; they are kept for experiment history, not for use.

- **Current entry point:** `../hero_rl_train.py`
- **Latest experiment:** `../hero_rl_train_v20.py` (BC + fine-tune, v3 action space)

Nothing here is imported by anything else. Safe to delete if the history
is no longer needed (it remains in git).
