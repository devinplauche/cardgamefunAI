# Cards Folder

This folder is a good place to store card art, card-specific scenes, or future card subclasses.

The sample project keeps card definitions in `data/cards.json` so beginners can add content without writing much code.
