[pytest]
# The merge brought in archive/legacy-prototype/, which carries its own
# `tests` package. Two packages named `tests` on the same rootdir collide and
# every test module fails to import. The live suite is tests/ only; the
# archived prototype is kept for reference, not run.
testpaths = tests
norecursedirs = archive .venv node_modules models* .git
